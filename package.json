{
  "name": "soul-winning-app",
  "version": "1.0.0",
  "description": "A contact management system for church soul winning outreach activities",
  "scripts": {
    "start": "cd client && npm install && npm start",
    "build": "cd client && npm install && npm run build",
    "test": "cd client && npm install && npm test",
    "install-deps": "cd client && npm install",
    "deploy": "cd client && npm install && npm run build && firebase deploy"
  },
  "keywords": [
    "soul-winning",
    "contact-management",
    "church",
    "evangelism",
    "outreach"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {}
}
